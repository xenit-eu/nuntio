allprojects {
    group 'be.vbgn.nuntio'

    repositories {
        mavenCentral()
    }

    plugins.withId('java-base') {
        java {
            sourceCompatibility = JavaVersion.VERSION_11
        }
    }
}
