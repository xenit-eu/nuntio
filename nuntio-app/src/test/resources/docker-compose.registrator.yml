version: '3.4'
services:
  registrator:
    image: hub.xenit.eu/public/registrator:7.1
    restart: on-failure
    depends_on:
      - dind
    environment:
      DOCKER_HOST: tcp://dind:2375
    command:
      - -explicit
      - consul://consul:8500
